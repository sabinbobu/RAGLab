"prompt_version": "v1",
      "id": 2,
      "experiment_id": "6ef5d7dc-cb29-4b43-9fd8-b7c1c4c4da43",



      "answer":

      "The core building blocks of AI systems are:\n\n1.

      Model call: Treated as a stateless function from prompt to completion.\n2.

      Tool use: Where the model emits structured requests to execute code, query a database, hit an API, or read a file, and the orchestrator runs those requests and feeds the results back.\n3. Memory and state: This can be as simple as appending messages to a list or as complex as a vector store with hierarchical structure (source, page 1).",


      "latency_ms": 3823.83
    }

"prompt_version": "v2",
      "id": 5,
      "experiment_id": "6ef5d7dc-cb29-4b43-9fd8-b7c1c4c4da43",
      "answer":


      "The core building blocks of AI systems are primarily three primitives.

      The first is the model call itself, which functions as a stateless operation that takes a prompt and produces a completion. The second is tool use, where the model generates structured requests to execute tasks like running code, querying databases, accessing APIs, or reading files.

      An orchestrator manages these requests and feeds the results back into the system. Lastly, there's memory and state, which can vary from simply appending messages to a list to using more complex setups like hierarchical vector stores.

       These components work together to create non-trivial AI systems (source: ai_orchestration.pdf, page 1).",


      "latency_ms": 4857.92
    }
